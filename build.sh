# command line for building the node js

# use nvm to select version
# build sources with nvm

# copy builded sources in node/ and npm/ directory
# make a tar.gz in dist directory
tar -cvzf  dist/node_0.12.1.tar.gz node/ npm/

